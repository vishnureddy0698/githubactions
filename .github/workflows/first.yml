name: Testing
on:
  push:
    branches: "main"
  workflow_dispatch:
jobs:
  workflowtesting:
    runs-on: centop
    steps:
      - uses: actions/checkout@v4
      - name: This step used for Testing
        run: echo "This is vishnu"
